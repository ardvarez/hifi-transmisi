mcpServers:
  alloydb-postgres:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-postgres"
      - "postgresql://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE>"
    env:
      PGHOST: "<HOST>"
      PGPORT: "<PORT>"
      PGUSER: "<USER>"
      PGPASSWORD: "<PASSWORD>"
      PGDATABASE: "<DATABASE>"
